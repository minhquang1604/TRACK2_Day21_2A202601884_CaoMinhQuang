# Sieu tham so cho mo hinh GradientBoostingClassifier
# Bo tham so nay duoc chon sau khi so sanh 5 lan chay tren MLflow UI (Buoc 1):
# dat f1_score cao nhat (0.7149) trong so cac lan chay, dat nguong f1_score >= 0.65.
n_estimators: 200
learning_rate: 0.1
max_depth: 5
